import React from "react";
import { LoadingOutlined } from "@ant-design/icons";

const Loader = (
  <LoadingOutlined
    style={{
      fontSize: "5rem",
      fontWeight: 600,
      margin: "4.8rem",
      color: "white",
    }}
    spin
  />
);

export default Loader;
